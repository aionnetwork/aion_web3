/*let chai = require('chai');
let assert = chai.assert;

let utils = require('../packages/web3-avm-abi/src/coder-utils');
let Abi = require('../packages/web3-avm-abi');

let abi = new Abi();


describe('AVM Contract Tests', function () {
    
    it('Testing methods...'+method.name, done => {
      abiMethodCall(method.name,method.inputs,method.output).then(res => {
        assert.deepEqual(res,test_cfg[arrData(method.output,method.name)],"Call Failed!")
        done();
      }).catch(err => {
        done(err);
      });
    });
    
});*/