/*
 * Configuration file for running integ tests; not an actual test.
 */

let config = {
    /*
    TEST_ACCT_ADDR: '',       // Populate with account with which deploy and send Txs
    TEST_ACCT_PW: 'password', // Populate with password for test account
    GAS: 0,                   // Populate with max gas for transactions (recommended: 4000000)
    GAS_PRICE: 0,             // Populate with gas price (recommended: 10000000000)
    */
}
module.exports = function() { 
    return config;
}();
